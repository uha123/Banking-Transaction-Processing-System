apiVersion: v1
kind: Service
metadata:
  name: banking-transaction-system
  labels:
    app: banking-transaction-system
spec:
  type: ClusterIP
  selector:
    app: banking-transaction-system
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: http
